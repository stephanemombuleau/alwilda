[[source]]

url = "https://pypi.python.org/simple"
name = "pypi"
verify_ssl = true


[requires]

python_version = "3.5"
# api star has a bug in 3.5.2, so we should use python version > 3.5.2, but we don't know how to set it in pipenv


[packages]

apistar = "==0.3.9"
cython = "*"
numpy = "*"
scipy = "*"
addr-detector = "*"


[dev-packages]
